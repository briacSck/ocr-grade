# Mistral setup

> Stub — fleshed out in Prompt 6.

- Create a Mistral account, generate an API key.
- Export `MISTRAL_API_KEY` in your shell profile (do NOT commit to the repo).
- Pin the model alias in `config.yaml` (default `mistral-ocr-latest`).
- Rotate the key quarterly; revoke immediately if leaked.
