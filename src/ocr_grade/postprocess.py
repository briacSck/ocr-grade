"""Markdown-aware cleanup and prompt/answer splitting.

Will hold `clean_text(ocr_result) -> CleanedPage` and
`split_prompt_and_answer(cleaned) -> (prompt_block, answer_block)`.
"""

# TODO(prompt7): implement clean_text() + split_prompt_and_answer().
