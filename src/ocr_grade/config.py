"""Typed settings loaded from config.yaml + environment variables.

Will hold the pydantic-settings `Settings` model (input/output/cache dirs,
DPI, course preset, redaction config, Mistral client config, price-per-page,
preprocess toggles) and a `load_settings(path)` helper.
"""

# TODO(prompt2): implement Settings model and load_settings().
